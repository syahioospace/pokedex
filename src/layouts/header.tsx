
const Header = () => {

  return (
    <header
      className=
        "sticky top-0 z-50 flex items-center bg-gray-0/80 px-4 py-4 backdrop-blur-xl dark:bg-gray-50/50 md:px-5 lg:px-6 2xl:py-5 3xl:px-8 4xl:px-10 card-shadow mb-5"
        //((isMounted && windowScroll.y) as number) > 2 ? "card-shadow" : "",
    >
      <div className="flex w-full max-w-2xl items-center">
      </div>
    </header>
  );
};

export default Header;
